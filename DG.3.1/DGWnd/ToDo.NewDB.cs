namespace DGWnd
{
  internal partial class ToDo
  {
    internal class NewDb
    {
      // ToDo:
      // Lookup & menuoption: one subclass (SqlObject)
      // Transfer Normalize procedure to classes
      // Value list for lookup: convert to SqlObject (create data driver)
      // GetSchema: 1 method на всі випадки
      // Unify (connection, sql command, parameter):
      // Make 1 class instead of several classes
      //  1. DbConnection
      //  2. DbSql 
      //  3. DbParameter  
   }
  }
}